const express = require('express');
const app = express();
const PORT = process.env.PORT || 3000;

// Avalik kaust staatiliste failide jaoks (nt HTML, CSS)
app.use(express.static('public'));

// Test-route
app.get('/', (req, res) => {
  res.send('Tere! Sinu server töötab edukalt 🚀');
});

app.listen(PORT, () => {
  console.log(`Server töötab: http://localhost:${PORT}`);
});
